"""
evals/a2_second_pass_chunk_additions.py — Phase A2 SECOND PASS (NOT YET AUTHORED).

Will handle the parts of A2 the first-pass script deliberately omits:
  - adding equivalent chunk_ids the original annotator missed
  - intentionally dropping chunks that turn out to be noise
  - re-validating sufficient_sets against the expanded chunk set

This requires reading the patch-notes corpus (chunk text) to confirm
semantic equivalence between candidate additions and existing concept
members. Doing this from case notes alone would be guessing — see the
first-pass script's docstring (evals/a2_first_pass_merge_rename.py) for
the rationale behind the split.
"""

raise NotImplementedError(
    "a2 second pass not yet authored — see this file's docstring and the "
    "first-pass script (evals/a2_first_pass_merge_rename.py)."
)
